# Append this file to the canonical `.env` in the minimal production deployment directory.
# Docker Compose then loads the complete file automatically; routine commands need no flags.
COMPOSE_PROJECT_NAME=web10-radio-prod
COMPOSE_FILE=compose.prod.yaml:compose.xray.yaml
WEB10_PROD_ENV_FILE=.env
WEB10_XRAY_ENV_FILE=.env
